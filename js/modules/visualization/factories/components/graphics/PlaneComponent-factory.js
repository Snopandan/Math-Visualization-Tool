VisualizationModule.factory('PlaneComponent', ['Component', 'Plane', function(Component, Plane) {
  var PlaneComponent = function(variable, value) {
    var that = Component();

    normal_ =
    showNormal_ =
    size_ =
    gridStep_ =

    return that;
  };

  return PlaneComponent;

}]);
